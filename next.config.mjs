/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com', 'bskilling-documents.s3.ap-south-1.amazonaws.com'],
    },
};

export default nextConfig;
